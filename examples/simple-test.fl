; simple-test.fl
[FUNC add :params [$a $b]
  :body (+ $a $b)
]

[FUNC main :params []
  :body (
    (let [[$result (add 3 4)]]
      (println (concat "3 + 4 = " (num-to-str $result)))
    )
  )
]

(main)
