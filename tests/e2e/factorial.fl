;; tests/e2e/factorial.fl — D4: Simple Recursion
;; Golden reference: basic recursive call without TCO

(defn factorial [$n]
  (if (<= $n 1)
    1
    (* $n (factorial (- $n 1)))))

(println (factorial 5))
