# FreeLang 코드 작성 진행 규칙

작성일: 2026-05-13  
적용 범위: `/root/kim/freelang-v11` 전체 (seed compiler, runtime, self-hosted)

---

## 1. 작업 진입 순서 (필수)

```
리서치 → 플랜모드 → 문서화 → 태스크 등록 → 구현 → 테스트/검증 루프
```

- **리서치**: 현재 코드 상태를 먼저 읽는다. 가정하지 않는다.
- **플랜모드**: 구현 전 설계를 명시적으로 정리한다.
- **문서화**: 설계 결정을 마크다운으로 남긴다 (ROADMAP.md 또는 별도 파일).
- **태스크 등록**: tasks.dclub.kr에 등록 후 시작한다.
- **구현 → 검증**: 구현 직후 반드시 실제 명령으로 검증한다.

---

## 2. 협의 원칙

- 모르거나 방향이 흔들리면 **혼자 결정하지 않는다** — 반드시 협의한다.
- 아키텍처 변경, Stage 설계 변경, 기존 ABI 변경은 협의 필수.
- "이렇게 해도 될 것 같다"는 혼자 판단으로 진행하지 않는다.

---

## 3. Stage 규칙

### 3-A. 단일 관심사

각 Stage는 **한 가지 기능만** 추가한다.

| Stage | 관심사 |
|-------|--------|
| S5 | FLVector + FLMap |
| S6 | loop/recur |
| S7 | closure (예정) |

벡터+맵+클로저를 동시에 구현하지 않는다. 진단이 불가능해진다.

### 3-B. 실패 시 재시작

중간단계(A/B/C/D) 하나라도 실패 시 **해당 Stage 처음부터 재시작**.  
부분 수정으로 넘어가지 않는다.

### 3-C. 검증은 실제 실행으로만

눈으로 코드 보고 "통과" 선언 금지.  
반드시 실제 명령을 실행하고 출력을 확인한다.

```bash
# 올바른 예
./freelang examples/loop.fl   # 실제 실행 후 출력 확인

# 금지
"코드를 보니 맞는 것 같습니다" → 통과 선언
```

---

## 4. 코드 품질 기준

### 4-A. 경고 0

```bash
gcc -Wall -Wextra -c runtime/runtime.c -o /dev/null   # 경고 0
gcc -Wall -Wextra freelang.c -o freelang               # 경고 0
```

경고가 있으면 다음 단계로 넘어가지 않는다.

### 4-B. 줄 수 제한

| 파일 | 한도 |
|------|------|
| `runtime/runtime.c` | 500줄 이하 |
| `freelang.c` | 600줄 이하 (기능 추가 시 재협의) |

초과 시 불필요한 함수/매크로 정리 후 재확인.

### 4-C. 코드 방향

- 추가보다 **줄이는 방향**을 기본으로.
- 사용하지 않는 함수/변수 즉시 제거.
- 미래 요구사항을 위한 추상화 금지 — 지금 필요한 것만.
- 비슷한 코드 3개가 생길 때만 공통화 검토.

### 4-D. 주석

- WHY가 명확하지 않은 코드에만 짧게 작성.
- "이 함수는 X를 한다" 류의 WHAT 주석 금지.
- 임시 우회 코드에는 반드시 이유를 남긴다.

---

## 5. 파일 수정 규칙

- **Edit 도구 사용** — `sed -i` 금지 (hook 감지 불가).
- 기존 파일 수정 전 반드시 Read로 현재 내용 확인.
- 새 파일은 꼭 필요한 경우에만 — 기존 파일 확장 우선.

---

## 6. 정식 해결 원칙

```
핫픽스 / 임시 우회 / 1줄 치환  →  금지
구조 정비 / 근본 리팩터         →  기본
```

- `--no-verify`, `--force` 금지 (명시적 요청 없으면).
- 에러가 나면 우회하지 말고 원인을 찾아 수정.

---

## 7. ABI 헌법 준수 (`self/ABI.md`)

- FLValue 레이아웃 변경은 ABI 헌법 개정 절차를 거친다.
- Phase A 메모리: malloc만, free 없음 — Phase B 이전까지 유지.
- Copy semantics: vec-push/vec-set/map-set은 새 객체 반환, 원본 불변.

---

## 8. 완료 후 처리 (Stage 단위)

```bash
# 1. 커밋
git add <변경 파일>
git commit -m "[S{N}] 제목"

# 2. Gogs 푸시
git push origin master

# 3. 블로그 포스팅
# blog.dclub.kr / 500자 이상 / 과정과 결과 솔직하게
```

셋 중 하나라도 빠지면 Stage 미완료.

---

## 9. 금지 패턴 요약

| 금지 | 대신 |
|------|------|
| `sed -i` 파일 수정 | Edit 도구 |
| 눈으로 보고 통과 선언 | 실제 명령 실행 후 출력 확인 |
| 여러 기능 한 Stage | Stage 분리 |
| 혼자 아키텍처 결정 | 협의 |
| 임시 우회 (hotfix) | 근본 원인 수정 |
| 미사용 코드 방치 | 즉시 제거 |
| 경고 무시하고 다음 단계 | 경고 0 후 진행 |
| `--no-verify` / `--force` | 원인 해결 |

---

## 10. 현재 seed compiler 지원 범위

| 기능 | 상태 |
|------|------|
| 리터럴 (int/float/string/bool/nil) | ✅ S4 |
| defn, define, let, if, cond, do | ✅ S4 |
| 산술·비교·논리 연산자 | ✅ S4 |
| println, print, str, file I/O | ✅ S4 |
| vector `[...]`, map `{...}`, vec-\*/map-\* | ✅ S5 |
| loop/recur | ✅ S6 |
| closure | ⏳ S7 예정 |
| try/catch | ⏳ 미정 |
